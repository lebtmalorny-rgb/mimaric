From 084a74f1e7096b66ee41903c75397813dbc5ce54 Mon Sep 17 00:00:00 2001
From: Lebt <lebtmalorny@gmail.com>
Date: Thu, 3 Sep 2026 17:04:45 +0300
Subject: [PATCH 8/8] feat: validate Horizon PowerOps runtime contract

---
 ansible/roles/horizon/tasks/deploy.yml        |   3 +
 ansible/roles/horizon/tasks/powerops.yml      |  19 ++
 ansible/roles/mistral/files/power_ops.yaml    |   9 +
 ansible/roles/mistral/tasks/powerops.yml      |   7 +-
 .../tests/unit/test_powerops_horizon.py       | 285 ++++++++++++++++++
 .../tests/unit/test_powerops_registration.py  |   7 +-
 6 files changed, 327 insertions(+), 3 deletions(-)
 create mode 100644 ansible/roles/horizon/tasks/powerops.yml
 create mode 100644 kolla_ansible/tests/unit/test_powerops_horizon.py

diff --git a/ansible/roles/horizon/tasks/deploy.yml b/ansible/roles/horizon/tasks/deploy.yml
index 2415269..5a9c9ef 100644
--- a/ansible/roles/horizon/tasks/deploy.yml
+++ b/ansible/roles/horizon/tasks/deploy.yml
@@ -12,3 +12,6 @@
 
 - name: Flush handlers
   meta: flush_handlers
+
+- import_tasks: powerops.yml
+  when: enable_horizon_powerops | bool
diff --git a/ansible/roles/horizon/tasks/powerops.yml b/ansible/roles/horizon/tasks/powerops.yml
new file mode 100644
index 0000000..14be18c
--- /dev/null
+++ b/ansible/roles/horizon/tasks/powerops.yml
@@ -0,0 +1,19 @@
+---
+- name: Verify Horizon PowerOps runtime settings
+  become: true
+  ansible.builtin.command: >-
+    {{ kolla_container_engine }} exec
+    --env POWEROPS_EXPECTED_REGION
+    --env POWEROPS_EXPECTED_PROJECT_NAMES
+    --env POWEROPS_EXPECTED_USER_NAMES
+    horizon python -c
+    "import json, os; os.environ.setdefault('DJANGO_SETTINGS_MODULE', 'openstack_dashboard.settings'); import django; django.setup(); import poweropsdashboard; import poweropsdashboard.enabled._50_powerops; from django.conf import settings; assert settings.POWEROPS_REGION_NAME == os.environ['POWEROPS_EXPECTED_REGION']; assert settings.POWEROPS_ALLOWED_PROJECT_NAMES == json.loads(os.environ['POWEROPS_EXPECTED_PROJECT_NAMES']); assert settings.POWEROPS_ALLOWED_USER_NAMES == json.loads(os.environ['POWEROPS_EXPECTED_USER_NAMES']); assert settings.POWEROPS_MOCK_MODE is False"
+  environment:
+    POWEROPS_EXPECTED_REGION: "{{ openstack_region_name }}"
+    POWEROPS_EXPECTED_PROJECT_NAMES: >-
+      {{ powerops_allowed_project_names | to_json }}
+    POWEROPS_EXPECTED_USER_NAMES: >-
+      {{ powerops_allowed_user_names | to_json }}
+  changed_when: false
+  run_once: true
+  delegate_to: "{{ groups['horizon'][0] }}"
diff --git a/ansible/roles/mistral/files/power_ops.yaml b/ansible/roles/mistral/files/power_ops.yaml
index 119be4e..c8da55a 100644
--- a/ansible/roles/mistral/files/power_ops.yaml
+++ b/ansible/roles/mistral/files/power_ops.yaml
@@ -3,6 +3,15 @@ version: '2.0'
 name: power_ops
 
 workflows:
+  host_inventory:
+    tasks:
+      inventory:
+        action: powerops.host_inventory
+        publish:
+          result: <% task().result %>
+    output:
+      result: <% $.result %>
+
   host_power_status:
     input:
       - host
diff --git a/ansible/roles/mistral/tasks/powerops.yml b/ansible/roles/mistral/tasks/powerops.yml
index 29137dd..5d87ab9 100644
--- a/ansible/roles/mistral/tasks/powerops.yml
+++ b/ansible/roles/mistral/tasks/powerops.yml
@@ -3,7 +3,7 @@
   become: true
   ansible.builtin.command: >-
     {{ kolla_container_engine }} exec {{ item.container }} python -c
-    "import importlib.metadata as m; required={'powerops.host_power_status','powerops.planned_power_off','powerops.planned_reboot','powerops.power_on_for_inspection','powerops.return_to_service'}; found={e.name for e in m.entry_points(group='mistral.actions')}; assert required <= found, required - found"
+    "import mistral; import mistral_lib; import importlib.metadata as m; required={'powerops.host_inventory','powerops.host_power_status','powerops.planned_power_off','powerops.planned_reboot','powerops.power_on_for_inspection','powerops.return_to_service'}; found={e.name for e in m.entry_points(group='mistral.actions') if e.name.startswith('powerops.')}; assert found == required, found ^ required"
   changed_when: false
   loop:
     - container: mistral_api
@@ -230,6 +230,7 @@
       {{ kolla_admin_openrc_cacert
          if kolla_admin_openrc_cacert else omit }}
   loop:
+    - powerops.host_inventory
     - powerops.host_power_status
     - powerops.planned_power_off
     - powerops.planned_reboot
@@ -260,6 +261,7 @@
       {{ kolla_admin_openrc_cacert
          if kolla_admin_openrc_cacert else omit }}
   loop:
+    - power_ops.host_inventory
     - power_ops.host_power_status
     - power_ops.planned_power_off
     - power_ops.planned_reboot
@@ -281,7 +283,8 @@
       - >-
         (powerops_action_reads.results | default([]) |
          map(attribute='json.name') | list | unique | sort) ==
-        ['powerops.host_power_status', 'powerops.planned_power_off',
+        ['powerops.host_inventory', 'powerops.host_power_status',
+         'powerops.planned_power_off',
          'powerops.planned_reboot', 'powerops.power_on_for_inspection',
          'powerops.return_to_service']
   no_log: true
diff --git a/kolla_ansible/tests/unit/test_powerops_horizon.py b/kolla_ansible/tests/unit/test_powerops_horizon.py
new file mode 100644
index 0000000..d2628e8
--- /dev/null
+++ b/kolla_ansible/tests/unit/test_powerops_horizon.py
@@ -0,0 +1,285 @@
+# Licensed under the Apache License, Version 2.0 (the "License"); you may
+# not use this file except in compliance with the License. You may obtain
+# a copy of the License at
+#
+#     http://www.apache.org/licenses/LICENSE-2.0
+#
+# Unless required by applicable law or agreed to in writing, software
+# distributed under the License is distributed on an "AS IS" BASIS,
+# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+# See the License for the specific language governing permissions and
+# limitations under the License.
+
+from pathlib import Path
+import unittest
+import warnings
+
+from ansible.parsing.dataloader import DataLoader
+from ansible.template import Templar
+import jinja2
+import yaml
+
+
+ROOT = Path(__file__).resolve().parents[3]
+GROUP_VARS = ROOT / "ansible/group_vars/all.yml"
+HORIZON_DEFAULTS = ROOT / "ansible/roles/horizon/defaults/main.yml"
+HORIZON_PRECHECK = ROOT / "ansible/roles/horizon/tasks/precheck.yml"
+HORIZON_DEPLOY = ROOT / "ansible/roles/horizon/tasks/deploy.yml"
+HORIZON_POWEROPS = ROOT / "ansible/roles/horizon/tasks/powerops.yml"
+MISTRAL_DEFAULTS = ROOT / "ansible/roles/mistral/defaults/main.yml"
+MISTRAL_REGISTER = ROOT / "ansible/roles/mistral/tasks/register.yml"
+MISTRAL_POWEROPS = ROOT / "ansible/roles/mistral/tasks/powerops.yml"
+SERVICE_REGISTER = ROOT / "ansible/roles/service-ks-register/tasks/main.yml"
+
+
+def _load_yaml(path):
+    return yaml.safe_load(path.read_text(encoding="utf-8"))
+
+
+def _normalise(value):
+    return " ".join(str(value).split())
+
+
+def _ansible_bool(value):
+    if isinstance(value, str):
+        return value.lower() in {"1", "on", "true", "yes"}
+    return bool(value)
+
+
+def _render(value, variables):
+    environment = jinja2.Environment(  # nosec: local expression fixture
+        undefined=jinja2.StrictUndefined,
+    )
+    environment.filters["bool"] = _ansible_bool
+    return environment.from_string(value).render(**variables)
+
+
+def _conditions_pass(conditions, variables):
+    templar = Templar(loader=DataLoader(), variables=variables)
+    with warnings.catch_warnings():
+        warnings.filterwarnings(
+            "ignore",
+            message="'crypt' is deprecated.*",
+            category=DeprecationWarning,
+            module="passlib.utils",
+        )
+        return all(
+            templar.template(
+                "{{ (%s) }}" % condition,
+                fail_on_undefined=True,
+            ) is True
+            for condition in conditions
+        )
+
+
+def _named(tasks, name):
+    matches = [task for task in tasks if task.get("name") == name]
+    if len(matches) != 1:
+        raise AssertionError(
+            f"expected one task named {name!r}, found {len(matches)}"
+        )
+    return matches[0]
+
+
+class HorizonPowerOpsDeploymentTest(unittest.TestCase):
+
+    def test_horizon_image_and_plugin_environment_are_derived(self):
+        services = _load_yaml(HORIZON_DEFAULTS)["horizon_services"]
+        horizon = services["horizon"]
+        image = horizon["image"]
+        expected = (
+            "{{ powerops_horizon_image ~ ':' ~ powerops_horizon_tag "
+            "if enable_horizon_powerops | bool else horizon_image_full }}"
+        )
+        self.assertEqual(_normalise(expected), _normalise(image))
+        self.assertEqual(
+            "upstream/horizon:2025.1",
+            _render(image, {
+                "enable_horizon_powerops": False,
+                "horizon_image_full": "upstream/horizon:2025.1",
+            }),
+        )
+        self.assertEqual(
+            "powerops-local/horizon:2025.1-powerops",
+            _render(image, {
+                "enable_horizon_powerops": True,
+                "powerops_horizon_image": "powerops-local/horizon",
+                "powerops_horizon_tag": "2025.1-powerops",
+                "horizon_image_full": "upstream/horizon:2025.1",
+            }),
+        )
+        environment = horizon["environment"]
+        self.assertEqual(
+            "{{ 'yes' if enable_horizon_powerops | bool else 'no' }}",
+            environment["ENABLE_POWEROPS"],
+        )
+        self.assertEqual(
+            "{{ 'yes' if enable_horizon_mistral | bool else 'no' }}",
+            environment["ENABLE_MISTRAL"],
+        )
+
+    def test_precheck_requires_shared_memcached_and_powerops_image(self):
+        tasks = _load_yaml(HORIZON_PRECHECK)
+        task = _named(
+            tasks,
+            "Validate Horizon PowerOps image and shared cache",
+        )
+        self.assertEqual("enable_horizon_powerops | bool", task["when"])
+        conditions = task["ansible.builtin.assert"]["that"]
+        combined = "\n".join(conditions)
+        for required in (
+                "enable_memcached",
+                "groups['memcached']",
+                "horizon_backend_database",
+                "powerops_horizon_image",
+                "powerops_horizon_tag"):
+            self.assertIn(required, combined)
+
+        valid = {
+            "enable_memcached": "yes",
+            "groups": {"memcached": ["cache-1", "cache-2"]},
+            "horizon_backend_database": False,
+            "powerops_horizon_image": "powerops-local/horizon",
+            "powerops_horizon_tag": "2025.1-powerops",
+        }
+        self.assertTrue(_conditions_pass(conditions, valid))
+        for override in (
+                {"enable_memcached": "no"},
+                {"groups": {"memcached": []}},
+                {"horizon_backend_database": True},
+                {"powerops_horizon_image": ""},
+                {"powerops_horizon_tag": "   "}):
+            with self.subTest(override=override):
+                values = dict(valid)
+                values.update(override)
+                self.assertFalse(_conditions_pass(conditions, values))
+
+    def test_keystone_role_object_is_created_without_assignment(self):
+        defaults = _load_yaml(MISTRAL_DEFAULTS)
+        expression = defaults["mistral_ks_roles"]
+        templar = Templar(
+            loader=DataLoader(),
+            variables={"enable_powerops": "yes"},
+        )
+        self.assertEqual(
+            ["powerops_operator"],
+            templar.template(expression, fail_on_undefined=True),
+        )
+        templar = Templar(
+            loader=DataLoader(),
+            variables={"enable_powerops": "no"},
+        )
+        self.assertEqual([], templar.template(
+            expression, fail_on_undefined=True
+        ))
+
+        register = _load_yaml(MISTRAL_REGISTER)[0]
+        variables = register["vars"]
+        self.assertEqual(
+            "{{ mistral_ks_roles }}",
+            variables["service_ks_register_roles"],
+        )
+        self.assertNotIn("service_ks_register_user_roles", variables)
+
+        mistral_text = "\n".join(
+            path.read_text(encoding="utf-8")
+            for path in (ROOT / "ansible/roles/mistral/tasks").glob("*.yml")
+        ).lower()
+        self.assertNotIn("role_assignment", mistral_text)
+        self.assertNotIn("implied", mistral_text)
+        self.assertIn("identity_role", SERVICE_REGISTER.read_text(
+            encoding="utf-8"
+        ))
+
+    def test_post_deploy_checks_are_read_only_and_cover_all_images(self):
+        mistral = _load_yaml(MISTRAL_POWEROPS)
+        check = _named(
+            mistral,
+            "Verify PowerOps Mistral action entry points",
+        )
+        command = _normalise(check["ansible.builtin.command"])
+        self.assertIn("import mistral", command)
+        self.assertIn("import mistral_lib", command)
+        self.assertIn("assert found == required", command)
+        self.assertEqual(
+            [
+                {"container": "mistral_api", "group": "mistral-api"},
+                {"container": "mistral_engine", "group": "mistral-engine"},
+                {
+                    "container": "mistral_executor",
+                    "group": "mistral-executor",
+                },
+            ],
+            check["loop"],
+        )
+
+        deploy = _load_yaml(HORIZON_DEPLOY)
+        flush_index = next(
+            index for index, task in enumerate(deploy)
+            if task.get("meta") == "flush_handlers"
+        )
+        imports = [
+            (index, task) for index, task in enumerate(deploy)
+            if task.get("import_tasks") == "powerops.yml"
+        ]
+        self.assertEqual(1, len(imports))
+        self.assertGreater(imports[0][0], flush_index)
+        self.assertEqual(
+            "enable_horizon_powerops | bool", imports[0][1]["when"]
+        )
+
+        tasks = _load_yaml(HORIZON_POWEROPS)
+        self.assertEqual(1, len(tasks))
+        command = _normalise(tasks[0]["ansible.builtin.command"])
+        self.assertIn(
+            "exec --env POWEROPS_EXPECTED_REGION "
+            "--env POWEROPS_EXPECTED_PROJECT_NAMES "
+            "--env POWEROPS_EXPECTED_USER_NAMES horizon python -c",
+            command,
+        )
+        self.assertIn("import poweropsdashboard", command)
+        self.assertIn("poweropsdashboard.enabled._50_powerops", command)
+        self.assertIn("django.setup()", command)
+        self.assertIn("settings.POWEROPS_REGION_NAME", command)
+        self.assertIn("settings.POWEROPS_ALLOWED_PROJECT_NAMES", command)
+        self.assertIn("settings.POWEROPS_ALLOWED_USER_NAMES", command)
+        self.assertIs(tasks[0]["changed_when"], False)
+        self.assertIs(tasks[0]["run_once"], True)
+        self.assertEqual("{{ groups['horizon'][0] }}", tasks[0]["delegate_to"])
+        self.assertEqual(
+            {
+                "POWEROPS_EXPECTED_REGION": "{{ openstack_region_name }}",
+                "POWEROPS_EXPECTED_PROJECT_NAMES": (
+                    "{{ powerops_allowed_project_names | to_json }}"
+                ),
+                "POWEROPS_EXPECTED_USER_NAMES": (
+                    "{{ powerops_allowed_user_names | to_json }}"
+                ),
+            },
+            {
+                name: _normalise(value)
+                for name, value in tasks[0]["environment"].items()
+            },
+        )
+
+        serialised = yaml.safe_dump(
+            mistral + tasks + deploy
+        ).lower()
+        for forbidden in (
+                "/executions",
+                "post /",
+                "put /",
+                "workflow start",
+                "workflow resume",
+                "baremetal node",
+                "server evacuate",
+                "server migrate",
+                "server stop",
+                "server start",
+                "masakari notification",
+                "etcdctl put"):
+            self.assertNotIn(forbidden, serialised)
+
+
+if __name__ == "__main__":
+    unittest.main()
diff --git a/kolla_ansible/tests/unit/test_powerops_registration.py b/kolla_ansible/tests/unit/test_powerops_registration.py
index 1c2e649..87c5682 100644
--- a/kolla_ansible/tests/unit/test_powerops_registration.py
+++ b/kolla_ansible/tests/unit/test_powerops_registration.py
@@ -30,6 +30,7 @@ MASAKARI_POWEROPS = ROOT / "ansible/roles/masakari/tasks/powerops.yml"
 WORKBOOK = ROOT / "ansible/roles/mistral/files/power_ops.yaml"
 
 ACTION_NAMES = [
+    "powerops.host_inventory",
     "powerops.host_power_status",
     "powerops.planned_power_off",
     "powerops.planned_reboot",
@@ -37,6 +38,7 @@ ACTION_NAMES = [
     "powerops.return_to_service",
 ]
 WORKFLOW_NAMES = [
+    "power_ops.host_inventory",
     "power_ops.host_power_status",
     "power_ops.planned_power_off",
     "power_ops.planned_reboot",
@@ -161,6 +163,8 @@ class PowerOpsRegistrationTest(unittest.TestCase):
         command = _normalise(check["ansible.builtin.command"])
         self.assertIn("exec {{ item.container }} python -c", command)
         self.assertIn("m.entry_points(group='mistral.actions')", command)
+        self.assertIn("if e.name.startswith('powerops.')", command)
+        self.assertIn("assert found == required", command)
         for name in ACTION_NAMES:
             self.assertIn(repr(name), command)
 
@@ -722,7 +726,7 @@ class PowerOpsRegistrationTest(unittest.TestCase):
     def test_workbook_is_exact_and_preserves_operator_return_gate(self):
         content = WORKBOOK.read_bytes()
         self.assertEqual(
-            "26c9f2a072827b5c342dcc1d51aacf5995110054a400efe3d68df0563f3e7921",
+            "cccb1255b1eacc0ba3b6a46ebbff5a4cc05954a53ae5c815bfe6afdb0ec29087",
             hashlib.sha256(content).hexdigest(),
         )
         workbook = yaml.safe_load(content)
@@ -734,6 +738,7 @@ class PowerOpsRegistrationTest(unittest.TestCase):
         )
 
         expected_actions = {
+            "host_inventory": ("inventory", "powerops.host_inventory"),
             "host_power_status": ("status", "powerops.host_power_status"),
             "planned_power_off": ("power_off", "powerops.planned_power_off"),
             "planned_reboot": ("reboot", "powerops.planned_reboot"),
-- 
2.49.0

